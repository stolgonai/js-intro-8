Array is a refference typa of data used for storing similar- multiple Data in a single variable



The type of Array it will return Object

ARRAY Indexes:
-Each Array element is stored with an index, started from 0
- You can use this index to get acces to data and manipulate with data\
- how to check the size of array is length of property

METHODS:
