const names = ["Alex", "John"];

//ADDING another elmnts at end PUSH

names.push("Max");
names.push("Anna", "Tolgonai", "Nurdin");

// POP

names.pop(2);
console.log(names);

// Shift removes the first elem from the beginning and return that element

const num = [10, 13, 17, 5, 2];

// unshift accept an arg, and add an el at the beginning of arr, and return a new length of arr
