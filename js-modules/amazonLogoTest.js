const { appUrl, ExpectedText } = require("./amaxonGlobalInfo");
console.log(ExpectedText.title);
console.log(ExpectedText.searchPlaceHolder);
/**
 * Test 1
 * Go to amazon.com
 * validate the logo is displayed
 */
